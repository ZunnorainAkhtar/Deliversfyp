class Address
{
  String placeFormattedAddress;
  String placeName;
  String placeId;
  double longitude;
  double latitude;

  Address({this.placeFormattedAddress, this.placeId, this.placeName, this.latitude, this.longitude});

}